enum UserStatus {
    ACTIVE
    BLOCKED
}

enum Role {
    TECHNICIAN
    CUSTOMER
    ADMIN
}

enum BookingStatus {
    REQUESTED
    ACCEPTED
    DECLINED
    IN_PROGRESS
    COMPLETED
    CANCELLED
}

enum PaymentMethod {
    CARD
    MOBILE_BANKING
}

enum PaymentProvider {
    STRIPE
    SSLCOMMEREZ
}

enum PaymentStatus {
    PENDING
    PAID
    FAILED
    CANCELLED
    REFUNDED
}
